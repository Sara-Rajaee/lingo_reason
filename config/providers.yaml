together_ai:
  api_key: ${TOGETHER_API_KEY}
  timeout: 600
  max_retries: 10

gemini:
  api_key: ${GEMINI_API_KEY}
  timeout: 600
  max_retries: 10

openai:
  api_key: ${OPENAI_API_KEY}
  timeout: 600
  max_retries: 3

cohere:
  api_key: ${COHERE_API_KEY}
  timeout: 600
  max_retries: 3

gpt_oss:
  api_base: ${GPT_OSS_API_BASE:-http://localhost:19743/v1/}
  api_key: ${GPT_OSS_API_KEY:-no-key}
  timeout: 600
  max_retries: 3
